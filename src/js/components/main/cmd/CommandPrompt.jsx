import React from "react";
import Iframe from "./Iframe.jsx";

const CommandPrompt = () => (
  <Iframe
    onInferredClick={(e) => { }}
  ></Iframe>
);

export default React.memo(CommandPrompt);
